# Exp1：X1 人形步态训练实验记录

## 实验索引

| 编号 | 日期 | 摘要 | 状态 |
|------|------|------|------|
| 5.1 | 2026-06-15 | 参考轨迹：半波正弦 → 升余弦包络 | 待训练 |
| 5.2 | 2026-06-16 | 参考轨迹：升余弦关节参考 → 最小 jerk 足端轨迹 + 简化 IK | 已修改，待重新训练 |
| 5.3 | 2026-06-16 | 在 5.2 基础上增大摆动膝屈曲、提高抬脚高度、缩短步长，尝试缓解步态抖动 | 已测试，步态周期过快 |
| 5.4 | 2026-06-16 | 修复 5.2/5.3 中发现的参考轨迹缺陷：支撑相位置、Z 轨迹、IK 精度、步长缩放 | **发现 IK Bug，左腿异常** |
| 5.5 | 2026-06-16 | 修复 5.4 IK Bug：右腿 hip_pitch 取负导致左右不对称，改为 Jacobian 映射方案 | 已测试，hip_yaw修复，但关节范围偏小 |
| 5.6 | 2026-06-17 | 针对 5.5 关节范围偏小问题：提高 ref_joint_pos 权重、降低 default_joint_pos 权重 | 已测试，严重抖动 |
| 5.7 | 2026-06-17 | 修复 5.6 抖动：恢复奖励权重 + 修复 IK 膝关节符号 Bug + 限制步长缩放 | 已测试，稳定但不对称+范围偏小 |
| 5.8 | 2026-06-18 | 修复 5.7 问题：提高 hip_yaw_roll_default 权重 + 增大 swing_height/step_length | 已测试，hip_roll修复但单腿驱动 |
| 7.0 | 2026-07-01 | 足跟-足尖滚动 + leg_symmetry；从 5.8 iter=3000 续训 | **已测试，对称略改善但速度崩溃** |
| 7.0t1 | 2026-07-01 | **消融 A**：仅保留减弱踝滚动 IK，关闭 leg_symmetry；从 5.8 重新续训 | **已测试，速度未恢复，滚动未实现** |

---

## 笔记与数据分析规范

**7.0 起实验笔记结构遵循 skill `czy/skills/skills/lab-notebook`；数据分析遵循 `czy/skills/skills/isaac-diag-eval`。**

| skill | 用途 |
| --- | --- |
| `lab-notebook` | 编号、索引表、实验章节模板、写回结论 |
| `isaac-diag-eval` | 跑 CSV 分析脚本，产出 E1~E8 数值 |

```powershell
$env:PYTHONIOENCODING='utf-8'
python czy/skills/skills/isaac-diag-eval/eval_isaac_diag.py czy/data/isaac_diag_<时间戳>.csv
python czy/skills/skills/isaac-diag-eval/eval_isaac_diag.py czy/data/isaac_diag_new.csv --compare czy/data/isaac_diag_old.csv --compare-label "5.8"
```

写回 §7 实验结果时优先引用：**E1 关节范围、E2 hip_roll 偏移、E3 位置域 HF%、第 7 节速度跟踪、第 8 节落地冲击**。

---

## 实验 5.1：升余弦参考包络（Raised Cosine）

---

## 实验 5.2：最小 Jerk 足端轨迹 + 简化 IK

### 1. 实验目的

在实验 5.1 的基础上，进一步把参考从“关节空间升余弦包络”升级为“足端空间最小 jerk 轨迹”，并通过简化 IK 映射到关节角，验证这种方式是否能够进一步降低前进过程中的抖动、改善摆脚平滑性，并提升回放观感。

### 2. 改动思路

与 5.1 相比，这次不再直接使用 `final_swing_joint_delta_pos` 生成关节参考，而是：

1. 基于 `phase` 区分左右腿摆动/支撑相位；
2. 对摆动腿生成最小 jerk 足端轨迹；
3. 再通过简化 IK 将足端轨迹映射为 12 维关节参考；
4. 训练仍沿用原有奖励结构，仅通过参考轨迹改变学习目标。

### 3. 本次改动文件

- `humanoid/envs/x1/x1_dh_stand_env.py`
  - 新增 `_min_jerk()`
  - 新增 `_solve_2d_leg_ik()`
  - 重写 `compute_ref_state()`
- `humanoid/envs/x1/x1_dh_stand_config.py`
  - 增加 `default_foot_pos`
  - 增加 `swing_height`
  - 增加 `step_length`
  - 增加 IK 相关参数

### 4. 改动性质

| 项 | 是否修改 |
|----|----------|
| 参考轨迹类型 | ✅ 关节空间参考 → 足端空间参考 |
| 轨迹平滑方式 | ✅ 升余弦 → 最小 jerk |
| IK 映射 | ✅ 新增简化 IK |
| 奖励函数 | ❌ 暂未改动 |
| 训练超参 | ❌ 暂未改动 |
| `play.py` | ❌ 不影响本次主改动 |

### 5. 当前配置快照

| 参数 | 值 |
|------|-----|
| `cycle_time` | 0.7 s |
| `swing_height` | 0.045 |
| `step_length` | 0.10 |
| `default_foot_pos` | `[[0.0, 0.10, -0.041], [0.0, -0.10, -0.041]]` |
| `use_ref_actions` | False |
| `max_iterations` | 3000（待训练确认） |

### 6. 风险与注意事项

- 当前 IK 为简化版本，不是严格的 X1 解析几何 IK；
- `default_foot_pos` 必须与机器人零位姿态一致，否则参考会偏；
- 若 `step_length` 或 `swing_height` 设得过大，可能导致摆脚不自然或训练不稳定；
- 实验二数据显示膝关节弯曲偏小，说明当前轨迹对膝屈曲的约束不够，需要在 IK / 参考轨迹里显式增加膝关节抬腿幅度；
- 训练前建议先小步长验证，再逐步放大步幅。

### 7. 实验结果（正式结论）

本次实验使用实验二的诊断数据 `czy/data/isaac_diag_20260616_122502.csv` 进行回放分析。结果表明：机器人虽然能够沿 `cmd_linear_x = 0.4` 的方向持续前进，但整体步态稳定性不足，存在较明显的周期性抖动与姿态波动；同时，摆腿阶段的膝关节屈曲幅度偏小，导致脚端抬摆不充分、落脚过渡不够自然，是影响稳定性的主要因素之一。

从数据上看：

- `cmd_linear_x` 全程保持在 `0.4`，说明命令输入稳定；
- `base_lin_vel_x` 平均约为 `0.41 m/s`，末段平均约 `0.405 m/s`，速度跟踪本身并不差；
- 但 `base_euler_x / base_euler_y` 仍存在可见波动，说明上身稳定性一般；
- 左右膝关节角度范围整体偏小，未形成足够的屈膝抬脚动作；
- 足底接触力峰值较高，且双脚接触状态切换不够平滑，说明落脚阶段仍有较强冲击或过渡不连续。

综合判断，本次“最小 jerk 足端轨迹 + 简化 IK”方案**尚未达到稳定行走目标**。虽然前进速度可以维持，但由于膝关节弯曲不足、脚步抬摆不充分以及 IK 近似带来的姿态误差，机器人步态表现出抖动、碎步和过渡生硬等问题。后续应优先增加摆动膝关节屈曲幅度，并对足端轨迹与 IK 模型进行进一步修正。

| 项 | 结果 |
|----|------|
| run / checkpoint | 待补充 |
| 训练 iter | 待补充 |
| 平均 vx | 约 `0.41 m/s` |
| rew_ref_joint_pos | 待补充 |
| 抖动情况 | 有明显抖动 |
| 现象描述 | 前进速度基本达标，但步态不稳，膝关节弯曲偏小，摆脚和落脚过渡不自然，足底接触冲击偏大。 |
| 结论 | 实验二验证了当前最小 jerk 足端轨迹 + 简化 IK 的可行性，但也暴露出膝屈曲不足和 IK 精度不足的问题；要获得稳定前进效果，需要进一步增大膝关节弯曲、优化足端轨迹，并替换为更精确的 IK。 |

### 8. 本次迭代修改记录（实验 5.3）

在实验 5.2 的基础上，针对“膝关节弯曲偏小、步态抖动、落脚过渡不自然”的问题，进一步做了以下调整：

1. 提高摆动相的膝关节屈曲幅度，使膝盖在摆动中段形成更明显的收腿动作；
2. 提高摆脚高度，减少脚尖擦地和抬脚不足的问题；
3. 缩短步长，优先追求稳定性而不是大步幅；
4. 保持最小 jerk 轨迹结构不变，避免引入新的高频抖动。

### 9. 5.3 当前状态说明

| 项 | 结果 |
|----|------|
| 修改目标 | 增大膝屈曲、提高抬脚高度、缩短步长 |
| 预期效果 | 降低抖动、减少擦地、让落脚更自然 |
| 训练状态 | 待复训 |
| 结论 | 这是对实验二的定向修正，重点验证“膝盖弯曲不足”是否为步态不稳的主因之一。 |

### 1. 实验目的

在**不改动奖励权重与训练超参**的前提下，仅替换 `compute_ref_state()` 中的关节参考包络函数，验证更平滑的摆动参考能否改善：

- `_reward_ref_joint_pos` 跟踪质量
- 摆动起止点的关节速度突变（PD 抖动）
- 步态相位与落脚的一致性

### 2. 对照基线（改动前）

**包络**：半波整流正弦 `|sin(2π·phase)|`

```python
sin_pos = sin(2π·phase)
左腿摆动 (sin < 0):  ref_delta = -clip(sin, max=0) · Δq
右腿摆动 (sin > 0):  ref_delta =  clip(sin, min=0) · Δq
|sin| < 0.1:         ref_delta = 0   # 双支撑
ref_dof_pos = ref_delta + default_dof_pos
```

**特点**：

- 摆动峰值在周期中点，与相位掩码一致
- 摆动起止点处包络导数不连续（尖角），易引起参考速度跳变
- 上升/下降对称，不符合「快抬慢落」的真实步态

### 3. 本次改动

**文件**：`humanoid/envs/x1/x1_dh_stand_env.py` → `compute_ref_state()`

**新包络**：升余弦（Hann 窗）

```
phase_mod = phase % 1
left_tau  = clamp(phase_mod - 0.5, min=0) × 2    # [0.5, 1.0] → [0, 1]
right_tau = clamp(phase_mod, max=0.5) × 2        # [0.0, 0.5] → [0, 1]

left_env  = (1 - cos(π·left_tau)) / 2
right_env = (1 - cos(π·right_tau)) / 2

|sin(2π·phase)| < 0.1 时 left_env / right_env = 0   # 双支撑死区保留

ref_delta[:, 0:6]  = left_env  · final_swing_joint_delta_pos[0:6]
ref_delta[:, 6:12] = right_env · final_swing_joint_delta_pos[6:12]
ref_action = 2 · ref_delta
ref_dof_pos = ref_delta + default_dof_pos
```

**改动性质**：

| 项 | 是否修改 |
|----|----------|
| 参考包络形状 | ✅ 半波正弦 → 升余弦 |
| `_get_phase()` / `cycle_time` | ❌ 不变 |
| `_get_stance_mask()` 逻辑 | ❌ 不变（仍用 sin 阈值 0.1） |
| `final_swing_joint_delta_pos` | ❌ 不变 |
| `use_ref_actions` | ❌ False |
| 奖励权重 / 训练配置 | ❌ 不变 |

### 4. 当前配置快照（与 5.0 相同）

| 参数 | 值 |
|------|-----|
| `cycle_time` | 0.8 s |
| `frame_stack` | 66 |
| `use_ref_actions` | False |
| `max_iterations` | 3000 |
| `lin_vel_x` | [-0.4, 1.2] |
| `ref_joint_pos` scale | 2.2 |
| `final_swing_joint_delta_pos` | [0.25, 0.05, -0.11, 0.35, -0.16, 0.0, -0.25, -0.05, 0.11, 0.35, -0.16, 0.0] |
| URDF | `X1_12DOF.urdf` |
| 地形 | trimesh（课程关闭） |

### 5. 预期效果

| 方面 | 预期 |
|------|------|
| 摆动起止 | 参考关节速度在触地/离地时为 0，减少边界抖动 |
| 跟踪奖励 | `rew_ref_joint_pos` 均值可能上升 |
| 步频/步幅 | 峰值幅度与基线相同（仍为 Δq），步频不应明显变化 |
| 风险 | 若策略已强拟合旧正弦参考，需重新训练或续训适应 |

### 6. 验收指标（play + CSV）

回放条件建议：`PLAY_CMD_VEL_X = 0.4`，记录 10 s @ 100 Hz。

| 指标 | 基线参考 | 5.1 目标 |
|------|----------|----------|
| 平均 vx（cmd=0.4） | — | ≥ 基线或持平 |
| 步态周期 | ~0.8 s | ~0.8 s |
| 相位–落脚对齐 | — | 不低于基线 |
| 关节参考跟踪误差 `‖dof_pos - ref_dof_pos‖` | — | 下降 |
| 支撑相脚弹跳（双踩） | — | 不恶化 |
| 视频观感 | — | 摆动更顺滑，无新增碎步 |

### 7. 训练与回放

```bash
# 新训练（实验 5.1）
cd humanoid
python scripts/train.py --task=x1_dh_stand --headless

# 回放 + CSV
python scripts/play.py --task=x1_dh_stand
```

日志目录：`logs/x1_dh_stand/`  
CSV 诊断：由 `play.py` 写入（路径见脚本内 `custom_save_path`）

### 8. 实验结果

> **待填**：训练完成后补充 checkpoint、CSV 文件名与关键指标。

| 项 | 结果 |
|----|------|
| run / checkpoint | — |
| 训练 iter | — |
| 平均 vx | — |
| rew_ref_joint_pos | — |
| 相位–落脚对齐 | — |
| 现象描述 | — |
| 结论 | — |

---

## 实验 5.4：修复参考轨迹缺陷（支撑相位置 + Z 轨迹 + IK 精度 + 步长缩放）

### 1. 实验目的

在实验 5.2/5.3 的基础上，修复代码审查发现的参考轨迹缺陷，验证这些修正能否进一步提升步态稳定性。

### 2. 修复内容

#### 问题 1：支撑相足端位置未随前进更新

**问题**：支撑相足端 X 坐标始终固定在 `default_foot_pos[0]`，但机器人前进时，支撑脚相对躯干应向后移动。

**修复**：
```python
# 支撑相：足端从前进位置（end）后退到起始位置（start）
left_stance_x = self._min_jerk(left_end[:, 0], left_start[:, 0], s_left)
left_foot[:, 0] = torch.where(left_swing, 
    self._min_jerk(left_start[:, 0], left_end[:, 0], s_left), 
    left_stance_x)
```

**影响**：策略现在会学习到正确的"支撑脚向后滑动"参考，减少躯干前进时的拖拽感。

#### 问题 2：Z 轨迹使用对称三角参数

**问题**：原代码使用 `2 * min(s, 1-s)` 作为 min_jerk 参数，导致 Z 轨迹在 s=1 时回到起点高度，而非落脚点高度。

**修复**：分为上升段和下降段分别计算：
```python
# 上升段 (s < 0.5)：从 start_z 到 mid_z
# 下降段 (s >= 0.5)：从 mid_z 到 start_z（落脚点高度）
left_swing_z = torch.where(s_left < 0.5,
    self._min_jerk(left_start[:, 2], left_mid_z, 2.0 * s_left),
    self._min_jerk(left_mid_z, left_start[:, 2], 2.0 * (s_left - 0.5)))
```

**影响**：摆动相 Z 轨迹现在更符合"抬脚-平移-落脚"的物理过程。

#### 问题 3：简化 IK 系数缺乏几何验证

**问题**：原 IK 使用经验线性系数 (-0.8, -1.2, 1.6, 0.6...)，与 URDF 几何参数不符，导致膝关节角度计算不准确。

**修复**：使用余弦定理计算精确几何 IK：
```python
# 余弦定理计算膝关节角度
cos_knee = (r**2 - L1**2 - L2**2) / (2 * L1 * L2)
knee_angle = torch.acos(torch.clamp(cos_knee, -1.0, 1.0))

# 髋关节角度
alpha = torch.atan2(z, x)
cos_hip_offset = (L1**2 + r**2 - L2**2) / (2 * L1 * r)
hip_pitch = alpha + torch.acos(torch.clamp(cos_hip_offset, -1.0, 1.0))

# 踝关节角度：保持足底水平
ankle_pitch = hip_pitch + knee_angle - torch.pi / 2
```

**影响**：IK 精度显著提升，膝关节角度更符合机器人几何约束。

#### 问题 4：步长与命令速度脱钩

**问题**：`step_length` 固定为 0.08，无论 `cmd_linear_x` 是 0.4 还是 1.2。

**修复**：
```python
cmd_vel = torch.abs(self.commands[:, 0])
step_length = self.step_length * torch.clamp(cmd_vel / 0.4, 0.5, 2.0)
```

**影响**：低速时步长自动缩短，避免碎步；高速时步长适当增大。

### 3. 改动文件

- `humanoid/envs/x1/x1_dh_stand_env.py`
  - 重写 `compute_ref_state()`（修复问题 1/2/4）
  - 重写 `_solve_2d_leg_ik()`（修复问题 3）
  - `__init__` 中新增 `ik_thigh_length`、`ik_shank_length` 属性

### 4. 当前配置快照

| 参数 | 值 |
|------|-----|
| `cycle_time` | 0.7 s |
| `swing_height` | 0.065 |
| `step_length` | 0.08（基础值，随速度缩放） |
| `ik_thigh_length` | 0.22 |
| `ik_shank_length` | 0.22 |
| `ik_hip_pitch_offset` | 0.0 |
| `ik_knee_offset` | 0.49 |
| `ik_ankle_pitch_offset` | -0.21 |
| `default_foot_pos` | `[[0.0, 0.10, -0.041], [0.0, -0.10, -0.041]]` |
| `use_ref_actions` | False |
| `max_iterations` | 3000 |

### 5. 风险与注意事项

- IK 几何参数 `ik_thigh_length` / `ik_shank_length` 需与 URDF 实际连杆长度对齐（当前配置为 0.22，URDF 测量值约为 0.14/0.30，建议训练前验证）
- 步长缩放范围 `[0.5, 2.0]` 为经验值，可根据训练效果调整
- 支撑相后退轨迹假设地面平坦，若地形不平需额外处理

### 6. 验收指标

| 指标 | 目标 |
|------|------|
| 平均 vx（cmd=0.4） | ≥ 0.35 m/s |
| 膝关节角度范围 | 明显大于实验 5.2 |
| 足底接触力峰值 | 低于实验 5.2 |
| 步态周期 | ~0.7 s |
| 视频观感 | 支撑脚滑动自然，摆腿起落平滑 |

### 7. 训练与回放

```bash
# 新训练（实验 5.4）
cd humanoid
python scripts/train.py --task=x1_dh_stand --headless

# 回放 + CSV
python scripts/play.py --task=x1_dh_stand
```

### 8. 实验结果

> **待填**：训练完成后补充。

| 项 | 结果 |
|----|------|
| run / checkpoint | — |
| 训练 iter | — |
| 平均 vx | — |
| 膝关节角度范围 | — |
| 足底接触力峰值 | — |
| 现象描述 | — |
| 结论 | — |

### 9. IK 参数验证（2026-06-16）

根据 URDF `X1_12DOF.urdf` 的关节 origin 计算：

| 关节链 | URDF 测量值 | 原配置值 | 更新后 |
|--------|-------------|----------|--------|
| hip_pitch → knee_pitch | 0.2678 m | 0.22 m | **0.2678** |
| knee_pitch → ankle_pitch | 0.3068 m | 0.22 m | **0.3068** |

**验证脚本**：`czy/exp1/verify_ik_geom.py`

**注意事项**：
- X1 的腿部关节布局非标准矢状面（hip_roll/hip_yaw 有偏移），IK 偏移量 `ik_hip_pitch_offset`、`ik_knee_offset`、`ik_ankle_pitch_offset` 需用于补偿
- `default_foot_pos Z = -0.041` 是相对于 ankle_roll 的偏移（脚底到踝关节距离），不是相对于髋关节

---

### 10. 备注

- 5.1为**单变量实验**：仅改参考包络，便于与半波正弦基线对比。
- 若 5.1 有效，后续可叠加：膝部相位超前、速度相关摆动幅度、`swing_ratio` 可调支撑/摆动占比。
- 升余弦与 `_get_stance_mask()` 仍共用 `sin(2π·phase)` 做双支撑判断，观测 `sin/cos(phase)` 无需修改。

---

## 实验 5.5：修复 IK Bug（右腿 hip_pitch 取负问题）

### 1. 问题发现

实验 5.4 回放时发现机器人**左腿姿态异常，左腿不朝前**。右腿正常。

通过数据分析发现左腿 `hip_yaw` 严重偏离默认值：

| 关节 | 左腿实际 | 默认值 | 偏差 |
|------|---------|--------|------|
| **hip_yaw** | **-1.050** | **-0.31** | **-0.74 rad (42°)** |
| hip_pitch | 0.042 | 0.4 | -0.358 |
| ankle_pitch | -0.388 | -0.21 | -0.178 |

策略输出的左腿 `action_hip_yaw = -1.678`，导致 `pos_des_raw = action * 0.5 + (-0.31) = -1.149`。

### 2. 根因分析

`pos_des_raw` 是策略输出的目标位置，不是 IK 参考轨迹。策略**主动学到了偏转左腿 yaw 的行为**。

**为什么会学这个？**
- 5.4 的几何 IK 对右腿 `hip_pitch` 取负（Bug），导致左右腿 pitch 参考严重不对称（左 0.036 vs 右 -0.276）
- 策略在训练中为了弥补 pitch 参考不合理，被迫通过偏转 hip_yaw 来补偿
- 形成了"左腿偏转 + 右腿正常"的畸形策略

### 3. Bug 定位

IK 函数 `_solve_2d_leg_ik()` 对右腿 `hip_pitch` 取负：

```python
return torch.stack((-hip_pitch, hip_roll, hip_yaw, knee_pitch, ankle_pitch, ankle_roll), dim=1)
```

同时，X1 腿部关节布局非标准矢状面，2D 几何 IK 无法正确处理
- 在错误的基础上取负，加剧了不对称

### 4. 修复方案（实验 5.5）

**1. IK 函数改为 Jacobian 映射**：放弃几何 IK，以默认关节角度为基础，根据足端偏移调整 pitch 关节，roll/yaw 保持默认。关键改动：
- 去掉右腿 `hip_pitch` 取负
- 左右腿独立从各自默认关节角度出发

**2. 增加奖励约束 hip_yaw/roll**：添加 `_reward_hip_yaw_roll_default()`，防止策略通过偏转 yaw/roll 补偿 pitch 误差。

### 5. 修改文件

- `humanoid/envs/x1/x1_dh_stand_env.py`
  - 重写 `_solve_2d_leg_ik()` 为 Jacobian 映射方案
  - 新增 `_reward_hip_yaw_roll_default()`
- `humanoid/envs/x1/x1_dh_stand_config.py`
  - 添加奖励权重 `hip_yaw_roll_default = 0.5`

### 6. 验收指标

- 左右髋pitch参考均值差异 < 0.05 rad
- 左腿 hip_yaw 实际值接近默认值 -0.31（偏差 < 0.1 rad）
- 回放时左腿姿态正常，朝前

### 7. 实验结果

数据来源：`isaac_diag_20260617_111152.csv`

| 项 | 结果 |
|----|------|
| run / checkpoint | 待补充 |
| **左腿 hip_yaw** | **-0.273**（目标 -0.31，偏差 0.037）✅ 修复成功 |
| **右腿 hip_yaw** | **0.296**（目标 0.31，偏差 0.014）✅ 修复成功 |
| 左右髋pitch均值差异 | 0.842 rad ⚠️（左 0.493 vs 右 -0.349） |
| 左膝范围 | **0.330 rad**（目标 >0.6）❌ 偏小 |
| 右膝范围 | **0.198 rad**（目标 >0.6）❌ 偏小 |
| 参考轨迹范围 | hip_pitch 1.3 rad, knee 0.7 rad（参考大）|
| 实际跟踪比例 | **30%-50%**（策略不跟踪参考）|
| base_euler std | roll=0.060, pitch=0.012 |
| 速度跟踪 | cmd=0.4, actual=0.337 (84%) |
| 现象描述 | 关节运动幅度小，步态保守，hip_yaw修复但整体效果不佳 |
| 结论 | hip_yaw Bug已修复，但策略**主动缩小运动幅度**，不跟踪大范围参考轨迹 |

---

### 8. 关节范围偏小问题分析

**数据对比**：

| 关节 | 参考范围 | 实际范围 | 跟踪比例 | 参考均值 | 实际均值 |
|------|---------|---------|---------|---------|---------|
| left_hip_pitch | 1.300 | 0.537 | **41%** | 0.471 | 0.493 |
| left_knee | 0.706 | 0.330 | **47%** | 0.462 | 0.566 |
| right_hip_pitch | 1.349 | 0.399 | **30%** | -0.308 | -0.349 |
| right_knee | 0.547 | 0.198 | **36%** | 0.398 | 0.467 |

**根因**：
- 参考轨迹范围足够大（IK/Jacobian 无问题）
- 但策略实际执行只有 30%-50%，**主动缩小幅度**
- 原因：奖励权重冲突（ref_joint_pos vs orientation vs default_joint_pos）

---

### 9. 经验总结

**教训**：
- `pos_des_raw` ≠ `ref_dof_pos`：前者是策略输出，后者是 IK 参考
- 策略会主动学习补偿参考不合理的行为（如偏转 yaw）
- 单看参考轨迹可能误导诊断，必须分析策略输出（action）
- **参考大 ≠ 实际大**：策略可能不跟踪参考

**建议**：
- 若腿部关节布局复杂，优先使用 Jacobian 映射而非几何 IK
- 对 yaw/roll 等非主要运动关节，应有额外奖励约束其接近默认值
- **参考轨迹生成后，必须验证策略是否跟踪**（对比参考范围 vs 实际范围）

---

## 实验 5.6：修复关节范围偏小问题

### 1. 问题继承

实验 5.5 发现：策略实际运动范围只有参考的 30%-50%，导致步态幅度小、速度不足。

### 2. 根因分析

**奖励权重冲突**：

| 奖励 | 权重 | 作用 |
|------|------|------|
| ref_joint_pos | 2.2 | 跟踪参考轨迹 |
| orientation | 1.0 | 保持躯干稳定 |
| default_joint_pos | 1.0 | 接近默认位置 |
| tracking_lin_vel | 1.8 | 速度跟踪 |

当参考轨迹要求大幅运动时：
- 策略跟踪参考 → 躯干不稳定 → 被 orientation 惩罚
- 策略跟踪参考 → 偏离默认位置 → 被 default_joint_pos 惩罚
- 策略选择折中：**缩小运动幅度，牺牲步态幅度换取稳定**

### 3. 修复方案

**方案 A：提高 ref_joint_pos 权重**
```python
# x1_dh_stand_config.py
class scales:
    ref_joint_pos = 4.0  # 从 2.2 提高到 4.0
    default_joint_pos = 0.5  # 从 1.0 降低到 0.5
    orientation = 0.8  # 降低，允许轻微躯干波动
```

**方案 B：改用动作缩放**
```python
# 直接在 IK 输出上乘以缩放因子
ref_dof_pos = ik_output * 1.5  # 放大参考轨迹
```

### 4. 修改文件

- `humanoid/envs/x1/x1_dh_stand_config.py`
  - 调整奖励权重

### 5. 验收指标

- 膝关节实际范围 > 0.6 rad
- 髋pitch实际范围 > 0.8 rad
- 参考跟踪比例 > 70%
- 速度跟踪 > 90%

### 6. 实验结果

> **待填**：训练完成后补充。

| 项 | 结果 |
|----|------|
| run / checkpoint | — |
| 膝关节范围 | — |
| 参考跟踪比例 | — |
| 速度跟踪 | — |
| 现象描述 | — |
| 结论 | — |

---

## 实验 5.6：提高 ref_joint_pos 权重（失败）

### 1. 目的

针对 5.5 中关节范围偏小的问题，尝试通过提高 `ref_joint_pos` 权重（2.2→4.0）、降低 `default_joint_pos`（1.0→0.5）和 `orientation`（1.0→0.8）来强制策略跟踪参考轨迹，增大运动幅度。

### 2. 参数变更

| 参数 | 5.5 | 5.6 |
|------|-----|-----|
| ref_joint_pos | 2.2 | **4.0** |
| default_joint_pos | 1.0 | **0.5** |
| orientation | 1.0 | **0.8** |

### 3. 实验结果

**数据文件**：`isaac_diag_20260617_144810.csv`

| 项 | 结果 |
|----|------|
| 速度跟踪 | 99% ✅ |
| 左膝实际范围 | 0.310 rad ❌（目标 >0.6） |
| 右膝实际范围 | 0.432 rad ❌ |
| 左膝高频能量(>5Hz) | **22.4%** ❌ 严重抖动 |
| 左膝加速度峰值 | **218 rad/s²** ❌ |
| base_ang_vel_x std | **0.427** ❌ 躯干剧烈晃动 |
| 步态周期 | 0.35s ❌ 高频碎步 |

### 4. 根因分析

**策略输出（pos_des_raw）范围爆炸**：

| 关节 | 5.5 输出范围 | 5.6 输出范围 | 实际范围 | 跟踪率 |
|------|-----------|-----------|---------|--------|
| left_hip_pitch | 1.300 | **2.841** | 0.446 | 15.7% |
| left_hip_roll | 0.328 | **2.838** | 0.329 | 11.6% |
| right_ankle_pitch | — | **2.535** | 0.269 | 10.6% |

**问题链条**：
1. `ref_joint_pos = 4.0` 过高，策略被迫输出大动作跟踪参考
2. PD 控制器无法执行如此剧烈的目标摆动
3. 目标与实际严重脱节（跟踪率仅 10-15%）→ 高频振荡
4. `hip_roll` 输出范围 2.8 rad 完全不合理（roll 应接近 0）

### 5. 结论

**提高 `ref_joint_pos` 权重是错误方向**。问题不在策略不够努力，而在：
1. 参考轨迹本身可能过于激进
2. PD 控制器带宽有限，无法跟踪高频大幅度目标
3. IK 膝关节符号 Bug 导致参考轨迹质量差

---

## 实验 5.7：修复 IK 符号 Bug + 恢复奖励权重

### 1. 目的

修复 5.6 抖动问题，回归稳定基线（5.5），同时修复 IK 中膝关节符号 Bug 以改善摆动相抬腿质量。

### 2. 变更内容

**变更 1：恢复奖励权重**

| 参数 | 5.6 | 5.7 |
|------|-----|-----|
| ref_joint_pos | 4.0 | **2.2**（恢复） |
| default_joint_pos | 0.5 | **1.0**（恢复） |
| orientation | 0.8 | **1.0**（恢复） |

**变更 2：修复 IK 膝关节符号 Bug**

```python
# 修复前（错误）：
knee_delta = -dz / L2 * 0.8  # 抬脚时dz>0，knee_delta<0 → 膝伸展（错误）

# 修复后（正确）：
knee_delta = dz / L2 * 0.8  # 抬脚时dz>0，knee_delta>0 → 膝屈曲（正确）
```

**影响**：摆动相峰值时，膝关节屈曲从 0.08 rad 增至 0.42 rad，抬腿动作更明显。

**变更 3：限制步长缩放范围**

```python
# 修复前：
step_length = self.step_length * torch.clamp(cmd_vel / 0.4, 0.5, 2.0)
# 高速时步长可达 0.16m，hip_pitch_delta 达 0.33 rad，过于激进

# 修复后：
step_length = self.step_length * torch.clamp(cmd_vel / 0.4, 0.3, 1.0)
# 步长最大 0.08m，hip_pitch_delta 最大 0.17 rad
```

### 3. 预期效果

- 消除抖动（恢复 5.5 的稳定基线）
- 膝关节屈曲更明显（符号 Bug 修复）
- 高速时步态更稳定（步长限制）
- 关节范围可能仍偏小，但优先保证稳定性

### 4. 验收指标

- 无高频抖动（>5Hz 能量 <10%）
- 膝关节屈曲在摆动相可见
- 速度跟踪 > 90%
- 躯干角速度 std < 0.2
- 对 yaw/roll 等非主要运动关节，应有额外奖励约束其接近默认值
- 参考轨迹生成后，应验证左右对称性（可添加单元测试）

### 5. 实验结果

**数据文件**：`isaac_diag_20260618_152415.csv`

| 项 | 结果 |
|----|------|
| 抖动（>5Hz 能量） | **0.9%** ✅ 完全消除 |
| 左膝实际范围 | 0.402 rad ❌（目标 >0.6） |
| 右膝实际范围 | 0.335 rad ❌ |
| 左髋 pitch 范围 | 0.445 rad ❌（目标 >0.8） |
| 右髋 pitch 范围 | 0.467 rad ❌ |
| 左髋 roll 偏移 | **0.26 rad** ❌ 严重不对称 |
| 右髋 roll 偏移 | **0.25 rad** ❌ |
| 速度跟踪 | 100% ✅ |

**结论**：抖动完全消除，但暴露两个新问题：
1. **左右腿不对称**：策略主动修改 hip_roll（action 均值 ±0.6），`hip_yaw_roll_default=0.5` 权重不足
2. **关节范围偏小**：参考轨迹幅度不足（swing_height=0.065, step_length=0.08）

---

## 实验 5.8：修复不对称 + 增大关节范围

### 1. 目的

修复 5.7 的两个问题：左右腿不对称和关节范围偏小。

### 2. 变更内容

**变更 1：提高 hip_yaw_roll_default 权重**

| 参数 | 5.7 | 5.8 |
|------|-----|-----|
| hip_yaw_roll_default | 0.5 | **2.0** |

**原因**：5.7 中策略 hip_roll action 均值达 ±0.6，偏移 0.25 rad，权重 0.5 完全无法约束。

**变更 2：增大参考轨迹幅度**

| 参数 | 5.7 | 5.8 | IK 输出变化 |
|------|-----|-----|-----------|
| swing_height | 0.065 | **0.10** | knee_delta: 0.21→0.33 rad |
| step_length | 0.08 | **0.12** | hip_pitch_delta: 0.20→0.29 rad |

**预期**：膝关节参考幅度从 0.46→0.58 rad，接近目标 0.6 rad。

### 3. 预期效果

- 左右腿 hip_roll/yaw 偏差 < 0.05 rad（当前 0.25 rad）
- 膝关节实际范围 > 0.5 rad（当前 0.33-0.40 rad）
- 保持无抖动

### 4. 验收指标

- hip_roll 偏移 < 0.05 rad
- 膝关节范围 > 0.5 rad
- >5Hz 能量 < 5%
- 速度跟踪 > 90%

### 5. 训练记录

| 项 | 值 |
|----|-----|
| `max_iterations` | 3000（**已完成**） |
| run / checkpoint | 待补充 run 名，`model_3000.pt` |
| 训练 iter | 3000 |
| 用途 | 作为实验 7.0 续训起点 |

### 6. 实验结果

**数据文件**：`isaac_diag_20260623_104848.csv`

| 项 | 结果 |
|----|------|
| hip_roll 偏移 | **0.035 rad** ✅ 大幅改善（5.7 为 0.26） |
| 左膝范围 | **0.604 rad** ✅ 达标 |
| 右膝范围 | **0.311 rad** ❌ 未达标 |
| 左髋pitch范围 | **0.794 rad** ✅ 改善 |
| 右髋pitch范围 | **0.360 rad** ❌ 偏小 |
| 左右膝范围比 | **1.9:1** ❌ 严重不对称 |
| 左右髋pitch范围比 | **2.2:1** ❌ 严重不对称 |
| 速度跟踪 | **80%** ⚠️ 下降 |
| >5Hz 能量 | 1.1% ✅ 无抖动 |

**结论**：`hip_yaw_roll_default=2.0` 成功约束 hip_roll，但出现**新问题——策略通过左腿驱动、右腿拖行**，左右腿运动幅度严重不对称（左膝范围是右膝的 1.9 倍）。

---

## 实验 7.0：足跟-足尖自然滚动 + 左右对称性

### 1. 上一实验结果与教训

> 数据：`isaac_diag_20260623_104848.csv`（5.8）
> - 左/右膝范围比 **1.9:1**（左 0.604 / 右 0.311 rad），单腿驱动
> - 左/右髋 pitch 范围比 **2.2:1**（左 0.794 / 右 0.360 rad）
> - hip_roll 偏移 **0.035 rad** ✅（5.7 为 0.26）
> - 速度跟踪 **80%** ⚠️；>5Hz 能量 **1.1%** ✅
>
> **核心教训**：`hip_yaw_roll_default=2.0` 可约束 hip_roll，但无法解决膝/髋 pitch 不对称；策略通过左腿驱动、右腿拖行。需显式对称奖励 + 踝关节滚动参考。

### 2. 本轮修改目标

- 左右腿运动幅度对称（膝 L/R < 1.2:1）
- 支撑相踝关节主动滚动，实现足跟-足尖自然步态

### 3. 修改内容

### 修改一：增强 IK 踝关节模型（支撑相滚动）

| 参数 | 旧值 | 新值 | 说明 |
| --- | --- | --- | --- |
| `stance_phase` | 无 | 新增 | 支撑相进度 0→1 传入 IK |
| `ankle_roll_amplitude` | 无 | 0.12 | 支撑相踝滚动幅度（rad） |

```python
# 支撑相：足跟-足尖滚动
stance_roll = ankle_roll_amplitude * sin(π * stance_phase)
# 摆动相：足尖上翘避免擦地
dorsiflex = -0.12 * sin(π * swing_phase)
```

**理由**：5.8 踝关节无主动滚动，落脚过渡不自然；支撑相滚动可改善足跟-足尖序列。

### 修改二：新增左右对称性奖励

| 参数 | 旧值 | 新值 | 说明 |
| --- | --- | --- | --- |
| `leg_symmetry` | 无 | 1.0 | `_reward_leg_symmetry()` 权重 |

```python
diff_hip = (left_hip_pitch - right_hip_pitch).abs()
diff_knee = (left_knee - right_knee).abs()
diff_ankle = (left_ankle - right_ankle).abs()
r = exp(-5 * (diff_hip + diff_knee + diff_ankle))
```

**理由**：5.8 膝/髋 L/R 比达 1.9~2.2，default 权重不足以约束对称。

### 4. 修改文件

- `humanoid/envs/x1/x1_dh_stand_env.py`
  - `_solve_2d_leg_ik()`：增加 `stance_phase` 与踝关节滚动
  - `compute_ref_state()`：计算支撑相进度
  - 新增 `_reward_leg_symmetry()`
- `humanoid/envs/x1/x1_dh_stand_config.py`
  - `leg_symmetry = 1.0`
  - `ankle_roll_amplitude = 0.12`

### 5. 训练参数

| 参数 | 值 |
| --- | --- |
| 训练方式 | **续训**（5.8 iter=3000） |
| `resume` | `True` |
| `load_run` | 5.8 run 名（待补充，参考 `2026-01-14_09-58-10test_20_video`） |
| `checkpoint` | `3000` |
| `max_iterations` | `3000`（累计 iter=6000） |
| 启动命令 | `python humanoid/scripts/train.py --task=x1_dh_stand --resume --load_run=<5.8_run> --checkpoint=3000` |

### 6. 预期与验收

| 指标 | 5.8 | 7.0 目标 | 异常信号 |
| --- | --- | --- | --- |
| 左右膝 L/R 比（E1） | 1.9 | < 1.2 | > 1.5 |
| 左右髋 pitch L/R 比（E1） | 2.2 | < 1.2 | > 1.5 |
| 双侧膝 range（E1） | 0.60/0.31 | > 0.5 | 任一侧 < 0.4 |
| hip_roll 偏移（E2） | 0.035 | < 0.05 | > 0.15 |
| 左膝 HF%（E3） | 1.1% | < 5% | > 10% |
| 速度跟踪（§7） | 80% | > 85% | < 75% |
| 落地冲击峰值（§8） | 待测 | < 300N | > 300N |
| 踝支撑相滚动 | 无 | > 0.1 rad | 不可见 |

### 7. 实验结果

> 工具：`isaac-diag-eval`
> 数据：`isaac_diag_20260701_160951.csv`（10s，100Hz，cmd=0.4 m/s）
> 对比：`--compare isaac_diag_20260623_104848.csv --compare-label "5.8"`

| 项 | 5.8 | 7.0 | 判定 |
| --- | --- | --- | --- |
| 左/右膝 range | 0.604 / 0.311 | 0.446 / 0.305 | 左膝↓，右膝≈ |
| 膝 L/R 比（E1） | **1.9** | **1.46** | ⚠️ 改善但未达标（<1.2） |
| 髋 pitch L/R 比（E1） | **2.2** | **1.39** | ⚠️ 改善但未达标 |
| hip_roll 偏移（E2） | 0.035 | **0.030** | ✅ 保持 |
| hip_yaw 偏移（E2） | — | **0.311** | ❌ 严重偏航 |
| 左膝 HF%（E3） | 1.1% | **2.0%** | ✅ 无位置域抖动 |
| 速度跟踪（§7） | **80%** | **26%** | ❌ 严重退化 |
| 落地冲击峰值（§8） | — | 1910N / 2620N | ❌ 硬着陆 |
| 稳定性 | — | max roll 5.6° | ✅ 未摔 |
| 航向漂移 | — | **+20.1°** | ⚠️ 明显跑偏 |
| 踝 vel HF 占比 | — | **0.85** | ❌ 高频抖动 |
| 踝 bang-bang 撞下限 | — | 左46% / 右42% | ❌ 限位边界振荡 |

| 项 | 结果 |
| --- | --- |
| run / checkpoint | 待补充 |
| 训练 iter | 续训至 6000（待确认） |
| 诊断 CSV | `isaac_diag_20260701_160951.csv` |

**结论**：❌ **未改进** — 相对 5.8，膝/髋 L/R 比从 1.9/2.2 降至 1.46/1.39（对称性方向正确但未达标），但**速度跟踪从 80% 崩至 26%**，机器人基本丧失有效前进能力；航向漂移 20°、踝 bang-bang 46%、PD 跟踪率仅 8~35% 表明策略与新增参考/奖励严重冲突。

**根因分析**：

1. **续训 + 大改参考/奖励**：在 5.8 权重上同时引入 IK 踝滚动 + `leg_symmetry`，策略未充分适应，输出目标（pos_des_raw）左右差异极大（如右踝 pitch des 低至 -2.1 rad）
2. **踝 bang-bang**：左右踝 pitch des 撞下限 40%+ → 速度域高频抖动 0.85，是抖动直接根因
3. **leg_symmetry 权重不足或形式问题**：膝 L/R 仅从 1.9→1.46，hip_yaw 反而出现 ±0.31 rad 对称性「镜像」偏移（左右反向偏），非真正协调步态
4. **速度崩溃**：`base_lin_vel_x` 均值 0.106 m/s（cmd 0.4），可能是策略保守化或步态周期紊乱

**附带发现**：

- 交替着地正常（29 次交替，双支撑 12%）
- 躯干稳定性优于 5.8（`base_ang_vel_x` std 0.42 vs 0.53）
- 位置域 HF% 仍低（E3 OK），问题主要在**策略输出幅度与限位冲突**，非 PD 高频振荡型抖动

**下一轮方向**（已拆分为 7.0t1 / 7.0t2 消融，见下文）：

1. ~~优先 7.0t1：提高 leg_symmetry + 降低踝参考~~ → 改为**先消融**再调参
2. 见 **实验 7.0t1**（仅踝滚动）与 **7.0t2**（仅对称奖励，待 t1 后）

---

## 实验 7.0t1：消融 A — 仅减弱踝滚动 IK（关闭 leg_symmetry）

### 1. 上一实验结果与教训

> 数据：`isaac_diag_20260701_160951.csv`（7.0）
> - 速度跟踪 **26%**（5.8 为 80%）❌ 策略几乎丧失前进能力
> - 膝 L/R **1.46**、髋 pitch L/R **1.39** ⚠️ 对称略改善但未达标
> - 踝 bang-bang 撞下限 **46%/42%**；PD 踝跟踪率 **<10%**
> - 支撑相踝 pitch **未呈现** sin(π·sp) 滚动波形（corr≈0.07~0.33）
>
> **核心教训**：
> - 7.0 **同时**引入 `leg_symmetry` + 踝滚动 IK，无法判断哪一项导致速度崩溃
> - 踝滚动参考虽写入 IK，但策略**未跟踪**，0.12 rad 幅度可能过大引发 bang-bang
> - **不应从 7.0 权重续训**（已崩坏），消融应回到 **5.8 iter=3000** 干净起点

### 2. 本轮修改目标

- **消融假设 H1**：关闭 `leg_symmetry` 后，仅保留减弱踝滚动 IK，速度可恢复至 5.8 水平（≥75%）
- **消融假设 H2**：`ankle_roll_amplitude` 减半后，支撑相踝 pitch 与 `sin(π·stance_phase)` 相关性提升，bang-bang 占比下降
- 本实验**不追求**对称性达标（留给 7.0t2）

### 3. 修改内容

### 修改三：关闭 leg_symmetry 奖励

| 参数 | 7.0 | 7.0t1 | 说明 |
| --- | --- | --- | --- |
| `leg_symmetry` | 1.0 | **0.0** | 消融：去掉对称奖励梯度 |

**理由**：排除对称奖励对速度崩溃的干扰，单独验证踝滚动 IK 是否可学。

### 修改四：减弱支撑相踝滚动幅度

| 参数 | 7.0 | 7.0t1 | 说明 |
| --- | --- | --- | --- |
| `ankle_roll_amplitude` | 0.12 | **0.06** | 减半，降低 des 撞限位风险 |

**理由**：7.0 踝 des 撞下限 46%；幅度减半后期望 bang-bang <30%，且仍保留足跟-足尖滚动参考（峰值 0.06 rad）。

### 修改五：减弱摆动相足尖上翘（代码内常数）

| 参数 | 7.0 | 7.0t1 | 说明 |
| --- | --- | --- | --- |
| `dorsiflex` 峰值 | -0.12 | **-0.08** | `x1_dh_stand_env.py` 摆动相上翘 |

```python
# x1_dh_stand_env.py _solve_2d_leg_ik()
dorsiflex = -0.08 * torch.sin(torch.pi * swing_phase)  # 原 -0.12
```

**理由**：与支撑相滚动同比例缩小，避免摆动相踝参考过激。

### 4. 修改文件

- `humanoid/envs/x1/x1_dh_stand_config.py`
  - `leg_symmetry = 0.0`
  - `ankle_roll_amplitude = 0.06`
- `humanoid/envs/x1/x1_dh_stand_env.py`
  - `dorsiflex` 峰值 -0.12 → -0.08
  - **保留** `stance_phase` 支撑相滚动逻辑（修改一不变）

### 5. 训练参数

| 参数 | 值 |
| --- | --- |
| 训练方式 | **续训（从 5.8，非 7.0）** |
| `resume` | `True` |
| `load_run` | 5.8 run（`2026-01-14_09-58-10test_20_video` 或等价路径） |
| `checkpoint` | `3000` |
| `max_iterations` | `3000`（累计 iter=6000） |
| 启动命令 | `python humanoid/scripts/train.py --task=x1_dh_stand --resume --load_run=<5.8_run> --checkpoint=3000` |

> ⚠️ **关键**：必须从 **5.8 `model_3000.pt`** 启动，不使用 7.0 训练产物。

### 6. 预期与验收

**主验证（消融 A）**：

| 指标 | 7.0 | 5.8 基线 | 7.0t1 目标 | 异常信号 |
| --- | --- | --- | --- | --- |
| 速度跟踪（§7） | 26% | 80% | **≥ 75%** | < 60% → H1 不成立 |
| 踝 bang-bang 撞下限 | 46% | — | **< 25%** | > 35% |
| corr(exp_sin, 踝ap) 支撑相 | ~0.07~0.33 | — | **> 0.5** | < 0.3 → 滚动仍未跟上 |
| 支撑相 ap_range | 0.16~0.17 | — | **0.06~0.12** | < 0.04 无滚动 |
| 左膝 HF%（E3） | 2.0% | 1.1% | < 5% | > 10% |
| hip_roll 偏移（E2） | 0.030 | 0.035 | < 0.05 | > 0.15 |

**次要（本实验不优化）**：

| 指标 | 7.0 | 7.0t1 预期 |
| --- | --- | --- |
| 膝 L/R 比 | 1.46 | 可能回弹至 ~1.7（无 symmetry 奖励） |
| hip_yaw 偏移 | 0.31 | 应 < 0.15（速度恢复后观察） |

**判定逻辑**：

| 结果 | 解读 | 下一步 |
| --- | --- | --- |
| 速度≥75% 且滚动 corr>0.5 | H1+H2 成立，踝滚动可单独学习 | 7.0t2 叠加 leg_symmetry |
| 速度≥75% 但滚动 corr<0.3 | 速度恢复来自去掉 symmetry，滚动仍无效 | 加大 `ref_joint_pos` 或从零训 |
| 速度仍 < 60% | 踝滚动 IK 本身破坏 5.8 策略 | 7.0t2 仅 symmetry；或回滚 IK 改动 |

### 7. 实验结果

> 工具：`isaac-diag-eval`
> 数据：`isaac_diag_20260701_181244.csv`（10s，100Hz，cmd=0.4 m/s）
> 对比：`--compare isaac_diag_20260623_104848.csv --compare-label "5.8"`

| 项 | 5.8 | 7.0 | 7.0t1 | 7.0t1 目标 | 判定 |
| --- | --- | --- | --- | --- | --- |
| 速度跟踪（§7） | 80% | 26% | **42%** | ≥75% | ❌ H1 不成立 |
| 膝 L/R 比（E1） | 1.9 | 1.46 | **1.71** | — | 无 symmetry 后回弹 |
| 髋 pitch L/R 比 | 2.2 | 1.39 | **1.58** | — | — |
| hip_roll 偏移（E2） | 0.035 | 0.030 | **0.026** | <0.05 | ✅ |
| hip_yaw 偏移（E2） | — | 0.311 | **0.307** | <0.15 | ❌ |
| 航向漂移 | — | +20.1° | **+35.1°** | — | ❌ 更差 |
| 左膝 HF%（E3） | 1.1% | 2.0% | **3.1%** | <5% | ✅ |
| 踝 bang-bang 撞下限 | — | 46%/42% | **32%/40%** | <25% | ❌ H2 未达标 |
| corr(exp_sin, 踝ap) | — | +0.33/+0.07 | **+0.02/-0.05** | >0.5 | ❌ 滚动未跟上 |
| 支撑相 ap_range | — | 0.17/0.16 | **0.19/0.14** | 0.06~0.12 | ⚠️ 有变化但非 sin 形 |
| 落地冲击峰值（§8） | — | 1910/2620N | **2014/1382N** | <300N | ❌ |
| 稳定性 | — | max roll 5.6° | **7.1°** | — | ✅ 未摔 |

| 项 | 结果 |
| --- | --- |
| run / checkpoint | 待补充 |
| 训练 iter | 续训至 6000（待确认） |
| 诊断 CSV | `isaac_diag_20260701_181244.csv` |

**结论**：❌ **消融 A 未通过** — 关闭 `leg_symmetry` 后速度从 7.0 的 26% **回升至 42%**（说明对称奖励是速度崩溃的部分原因），但仍远低于 5.8 的 80%；踝 bang-bang 从 46% 略降至 32~40%，**corr(exp_sin, 踝ap) 降至 ≈0**（比 7.0 更差），**足跟-足尖滚动仍未实现**。判定：**踝滚动 IK 本身干扰 5.8 策略**，即使幅度减半也无法在续训框架下学会。

**根因分析**：

1. **H1 部分成立**：去掉 `leg_symmetry` 后速度 26%→42%，说明 7.0 速度崩溃是「踝 IK + symmetry」叠加效应，symmetry 贡献约一半退化
2. **H2 不成立**：减弱至 0.06 rad 后策略仍不跟踪参考滚动，corr≈0；PD 踝跟踪率左 16%/右 3%
3. **踝 IK 续训不可行**：从 5.8 权重续训时，任何支撑相踝 pitch 增量都会扰动已收敛步态；右踝 des 仍低至 -1.86 rad，bang-bang 40%
4. **航向恶化**：yaw 漂移 35°，hip_yaw 镜像偏移 ±0.31 rad 依旧，与 7.0 同量级

**消融判定（对照 §6 表）**：

| 条件 | 实际 | 判定 |
| --- | --- | --- |
| 速度 ≥75% 且滚动 corr>0.5 | 42%，corr≈0 | ❌ |
| 速度 ≥75% 但 corr<0.3 | 速度也未达标 | ❌ |
| 速度 <60% | 42%（介于 26% 与 80% 之间） | ⚠️ 部分恢复但仍失败 |

**下一轮方向**：

1. **优先 7.0t2**：从 5.8 续训，**仅** `leg_symmetry=1.0`，**关闭踝滚动 IK**（`ankle_roll_amplitude=0` 或禁用 `stance_phase`）— 验证对称奖励单独效果
2. **放弃续训踝 IK 路线**：若 t2 速度恢复且对称改善，踝滚动应改为**从零训练**或**纯参考项**（提高 `ref_joint_pos`）而非在 5.8 上硬续
3. 暂不建议 7.0t3 继续加大踝幅度或叠加 symmetry

---

## 实验 7.0t2：消融 B — 仅 leg_symmetry（待开训）

> **状态**：7.0t1 已完成，**建议立即开训**。7.0t1 证明踝滚动 IK 续训路线不可行；t2 验证对称奖励单独效果。

### 1. 上一实验结果与教训（7.0t1）

> 数据：`isaac_diag_20260701_181244.csv`
> - 关闭 symmetry 后速度 **42%**（7.0 为 26%，5.8 为 80%）
> - 踝滚动 corr **≈0**，bang-bang **32~40%**
>
> **教训**：踝滚动 IK 无法通过 5.8 续训学会；应单独测试 `leg_symmetry` 并**回滚踝 IK**。

### 设计摘要

| 项 | 内容 |
| --- | --- |
| 起点 | 5.8 iter=3000 |
| 改动 | `leg_symmetry=1.0`，`ankle_roll_amplitude=0`（禁用支撑相滚动），恢复 `dorsiflex=-0.12` 或置 0 |
| 目的 | 验证对称奖励能否在不改踝 IK 的情况下改善 L/R 比且保持速度 |
| 触发条件 | ~~等 7.0t1~~ **已满足，可开训** |

### 预期对照矩阵

| 实验 | 踝滚动 IK | leg_symmetry | 回答的问题 |
| --- | --- | --- | --- |
| 5.8 | ❌ | ❌ | 基线 |
| 7.0 | ✅ 0.12 | ✅ 1.0 | 全量（失败） |
| **7.0t1** | ✅ 0.06 | ❌ | 滚动单独贡献？ |
| **7.0t2** | ❌ | ✅ 1.0 | 对称奖励单独贡献？ |
